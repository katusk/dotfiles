(defcfg
  process-unmapped-keys yes
)

(defsrc
  caps ;
)

(defvar
  tap-time  150
  hold-time 200
)

(defalias
  caps (multi f24 (tap-hold $tap-time $hold-time caps lctl))
  ;    (multi f24 (tap-hold $tap-time $hold-time ;    rctl))
)

(deflayer base
  @caps @;
)
